#include "minirt.h"
//not using this right now..

/* t_vec3	get_light_dir(t_vec3 light, t_vec3 object)
{
	t_vec3 light_dir;
	
	light_dir.x = light.x - object.x;
	light_dir.y = light.y - object.y;
	light_dir.z = light.z - object.z;
	return (normalize_vec(light_dir));
} */